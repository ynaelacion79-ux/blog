# Copy this file to .env and fill values
PORT=4000
JWT_SECRET=your-very-strong-secret
CORS_ORIGIN=http://localhost:8080

# PostgreSQL connection (set these to match your local Postgres installation)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=wendells_recipe
DB_USER=wendell
DB_PASSWORD=strongpassword

# Note: For production, use a secure secret and do not commit .env to VCS
